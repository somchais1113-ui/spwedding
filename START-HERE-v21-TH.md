# PS Wedding v21 — คำอวยพรเบื้องหลัง, Assist และ Analytics

## อ่านส่วนนี้ก่อน

แพ็กเกจนี้ยังไม่ได้ติดตั้งบนเว็บจริง การอัปเดต **ต้องทำทั้งเว็บไซต์และ Apps Script คำอวยพร** พร้อมเปิดตัวประมวลผลหนึ่งครั้งตามลำดับด้านล่าง ใช้ชีต โฟลเดอร์ ลิงก์ /exec และรหัสลับเดิมได้ ไม่ต้องสร้างระบบ RSVP ใหม่

เอกสารนี้ใช้แทนขั้นตอนคำอวยพรในคู่มือรุ่นเก่า ไฟล์ START-HERE-WISHES-TH.md เก็บไว้ให้อ้างอิงเท่านั้น

## สิ่งที่เปลี่ยน

- เมื่อแขกส่งข้อความหรือลายมือ เว็บส่งข้อความและจุดเส้นลายมือ แทนการสร้างและอัปโหลด PNG ขนาดใหญ่บนโทรศัพท์ก่อน
- ระบบบันทึกต้นฉบับลง Drive และแถวใน Sheets ให้เสร็จก่อนแสดงข้อความว่าได้รับคำอวยพรแล้ว เมื่อเห็นข้อความยืนยันนี้ แขกจึงปิดหน้าเว็บได้
- ตัวประมวลผลที่ Google เรียกตามเวลาจะสร้างภาพผ่าน Vercel แล้วเก็บภาพใน Drive ต่อเอง ไม่ต้องใช้หน้าจอของแขก
- การ์ดยังคงขนาดแนวตั้ง 2400×3000 / แนวนอน 3200×1800 พร้อมข้อมูล 300 DPI และ PNG แบบไม่สูญเสียรายละเอียด ลายมือต้นฉบับเก็บจุดเส้นไว้ครบตามข้อมูลที่ส่ง
- ปุ่มส่งซ้ำใช้รหัสรายการเดิม งานที่เกิดปัญหาลองใหม่ได้สูงสุด 5 ครั้ง แล้วแจ้งให้เจ้าของตรวจสอบ ต้นฉบับยังคงอยู่
- เพิ่มนก Assist ที่ Landing Page พร้อมแผนที่ ห้องพัก LINE และกำหนดการ มีการขยับเบา ๆ และลดการเคลื่อนไหวตามการตั้งค่าอุปกรณ์
- ติดตั้ง @vercel/analytics สำหรับเว็บ HTML/JavaScript นี้ด้วย inject() ไม่ใช่คอมโพเนนต์ Next.js เก็บเฉพาะ page views โดยไม่ส่งชื่อ คำอวยพร หรือลายมือเป็น event

**เวลารับข้อมูลยังขึ้นกับอินเทอร์เน็ตและ Google จึงยังรับประกันจำนวนวินาทีไม่ได้** สิ่งที่ลดออกจากเวลารอของแขกคือการสร้างภาพและอัปโหลดภาพทั้งหมด การสร้างภาพใน Drive อาจเริ่มหลังรอบตัวประมวลผลประมาณหนึ่งนาที และอาจนานขึ้นเมื่อมีหลายรายการหรือเจอโควตา

## 1. สำรองก่อนอัปเดต

1. เก็บ ZIP/โค้ดเว็บรุ่นที่ใช้อยู่ และสำเนา Code.gs ของ Apps Script คำอวยพรไว้
2. เปิด Apps Script โปรเจกต์ **คำอวยพร** เดิมที่ใช้กับ WISHES_SCRIPT_URL อย่าเปิดโปรเจกต์ RSVP
3. ใน Project Settings → Script properties ตรวจว่าค่าเดิมยังอยู่: WISHES_SHEET_ID, WISHES_DRIVE_FOLDER_ID และ WISHES_SHARED_SECRET ไม่ต้องเปลี่ยนรหัสลับ

ปลายทางเดิม:

- Sheets: https://docs.google.com/spreadsheets/d/1He7PKjI5SvLfFaqupgbHIghvl7S2nM_QUg6JWdzztt0/edit
- Drive: https://drive.google.com/drive/folders/1Cp5hMHU6-OCN7xgqQhsO_bHgMlHPlXdu

## 2. อัปเดต Apps Script เดิม

1. เปิดไฟล์ `integrations/google-wishes/Code.gs` จากแพ็กเกจนี้ คัดลอกทั้งหมดไปแทน Code.gs ในโปรเจกต์คำอวยพรเดิม แล้ว Save
2. ใน Project Settings เปิด “Show appsscript.json manifest file in editor”
3. กลับ Editor เปิด appsscript.json แล้วใช้เนื้อหาจาก `integrations/google-wishes/appsscript.json` แทน สำหรับโปรเจกต์คำอวยพรโดยเฉพาะนี้ ไฟล์เพิ่มสิทธิ์เรียกเว็บและสร้าง time trigger ให้ระบบใหม่
4. กด **Deploy → Manage deployments → เลือก Web app เดิม → Edit (ดินสอ) → Version: New version → Deploy**
5. คง Execute as = Me และ Who has access = Anyone ตามระบบเดิม เว็บไซต์ยังส่งรหัสลับตรวจสิทธิ์ทุกคำขอ
6. หาก Google ขออนุญาตเพิ่ม ให้ตรวจว่าเป็นโปรเจกต์ของคุณและอนุญาตสิทธิ์ที่แสดง หากบัญชีองค์กรห้าม ให้ผู้ดูแลบัญชีจัดการ
7. การแก้ deployment เดิมจะคง URL ลงท้าย /exec เดิมไว้ ถ้าเผลอสร้าง deployment ใหม่ ต้องแก้ WISHES_SCRIPT_URL ใน Vercel และ Redeploy ด้วย

**ยังไม่รัน setupWishesQueue จนกว่าเว็บไซต์ในขั้นตอน 3 จะ Ready** เว็บรุ่นก่อนยังส่งด้วยเส้นทางเดิมได้ แต่เว็บ v21 จะไม่ยืนยันรับรายการใหม่จนเปิดระบบเบื้องหลังครบ ควรทำขั้นตอน 2–4 ต่อเนื่องในช่วงที่แขกยังไม่ได้ใช้งาน

## 3. อัปเดตเว็บไซต์ใน Vercel

1. แตก ZIP และนำเนื้อหาแพ็กเกจไปอัปเดตโปรเจกต์เว็บเดิม รวม `package.json`, `package-lock.json`, `api`, `lib`, `js`, `src`, `assets`, `tools` และ `vercel.json` ไม่ใช่อัปโหลดเฉพาะ index.html
2. ถ้าใช้ Git ให้อัปเดต repository เดิมแล้ว push ตามวิธีที่ใช้อยู่ ถ้าใช้ CLI ให้รันจากโฟลเดอร์ที่มี package.json ด้วย `vercel --prod` ผ่านบัญชีและโปรเจกต์เดิม
3. ค่าตั้งโปรเจกต์: Framework = Other, Build Command = `node tools/build-web.cjs`, Output Directory = `public`, Node.js 22 หรือใหม่กว่า ใช้ npm install อัตโนมัติตาม package-lock.json อย่าใช้ Install Command ที่ข้ามการติดตั้งแพ็กเกจ
4. ใน Settings → Environment Variables คง **Production** ของ WISHES_SCRIPT_URL และ WISHES_SHARED_SECRET ให้ตรงกับ Apps Script เดิม คงตัวแปร RSVP ทั้งหมดไว้
5. ฟังก์ชันสร้างภาพตั้ง maxDuration 300 วินาทีไว้ใน vercel.json หากบัญชีแจ้งว่าไม่รองรับเวลานี้ ให้เปิด Fluid Compute ในการตั้งค่าฟังก์ชันและ Deploy ใหม่ก่อนเปิดคิว
6. รอ deployment ขึ้น Ready แล้วเปิดโดเมน Production จริง

ไม่ต้องใส่รหัสลับลงโค้ดหน้าเว็บหรือส่งรหัสลับในแชต

## 4. เปิดตัวประมวลผลเบื้องหลังครั้งเดียว

1. กลับ Apps Script คำอวยพรเดิม
2. เลือกฟังก์ชัน **setupWishesQueue** จากรายการด้านบน แล้วกด Run
3. อนุญาตสิทธิ์เพิ่มถ้าถูกถาม ระบบจะตรวจการเชื่อมต่อกับ Vercel แบบมีรหัสลับก่อนติดตั้ง
4. รอ Execution completed
5. เปิดเมนู Triggers (รูปนาฬิกา) จะเห็น **processWishesQueue** แบบ Time-driven ทุก 1 นาทีหนึ่งรายการ ไม่ต้องสร้างซ้ำด้วยมือ
6. เปิด Drive จะพบโฟลเดอร์เพิ่มชื่อ **ต้นฉบับคำอวยพร** ค่า WISHES_SOURCE_FOLDER_ID และ WISH_QUEUE_READY ถูกสร้างอัตโนมัติใน Script properties ไม่ต้องกรอกเอง

รัน setupWishesQueue ซ้ำได้ ระบบไม่ตั้ง trigger ซ้ำสำหรับบัญชีที่รัน แนะนำให้ใช้บัญชีเจ้าของเดิมบัญชีเดียวติดตั้งและดูแล

ระบบตั้งปลายทางสร้างภาพไว้ที่ `https://spwedding-teal.vercel.app/api/wish-render` แล้ว หากเปลี่ยนโดเมน ให้เพิ่ม Script property ชื่อ **WISHES_RENDER_URL** เป็น `https://โดเมนใหม่/api/wish-render` และรัน setupWishesQueue ใหม่ ใช้ Production URL ที่ Google เข้าถึงได้ ไม่ใช่ Preview ที่ต้องล็อกอิน Vercel

## 5. ทดสอบจริงก่อนแจ้งแขก

1. เปิดเว็บ Production ใหม่บนโทรศัพท์ ส่งข้อความสั้นชื่อ “ทดสอบข้อความ v21”
2. รอข้อความ **ได้รับคำอวยพรแล้ว…ปิดหน้านี้ได้เลย** จากนั้นปิดแท็บ
3. เปิด Google Sheets แท็บ “คำอวยพร” ต้องพบรายการแล้ว และ Drive/ต้นฉบับคำอวยพร ต้องมีไฟล์ชื่อผู้ส่งตามด้วยรหัสรายการ
4. รอรอบประมวลผลหรือเลือก processWishesQueue → Run ใน Apps Script เพื่อทดสอบทันที ภาพจะลงโฟลเดอร์ “การ์ดคำอวยพร” ชีตมีลิงก์ภาพและสถานะ “บันทึกครบแล้ว”
5. ทำซ้ำด้วยชื่อ “ทดสอบลายมือ v21” วาดเส้นแล้วส่ง ปิดแท็บหลังยืนยัน ตรวจทั้ง “ลายมือต้นฉบับ” และ “การ์ดคำอวยพร”
6. แตะ Assist ตรวจแผนที่ ห้องพัก LINE ทั้งสองรายการ และกำหนดการ ตรวจปิดด้วยปุ่ม × หรือแตะนอกเมนู
7. เปิดการ์ดเชิญและปิดกลับหน้าเดิม ตรวจทั้งมือถือและ Desktop

อย่าแสดงให้แขกเห็นว่าสำเร็จจากแค่การกดปุ่ม หากยังไม่เห็นข้อความยืนยัน ระบบยังไม่ยืนยันว่ารับข้อมูลแล้ว หากขึ้นข้อผิดพลาดให้กดส่งอีกครั้ง ระบบใช้รหัสเดิมเพื่อป้องกันรายการซ้ำ

## ข้อมูลอยู่ตรงไหน

| ข้อมูล | ปลายทาง |
| --- | --- |
| ชื่อ ข้อความ วิธีเขียน และสถานะ | ชีต “คำอวยพร” เดิม |
| ต้นฉบับข้อความ/จุดเส้นลายมือ | Drive → ต้นฉบับคำอวยพร → ชื่อผู้ส่ง_รหัสรายการ_ต้นฉบับ.json |
| ภาพการ์ดพร้อมใช้ทำคอนเทนต์ | Drive → การ์ดคำอวยพร และลิงก์ในชีต |
| ภาพเส้นลายมือ | Drive → ลายมือต้นฉบับ และลิงก์ในชีต |

ชื่อไฟล์มีชื่อผู้ส่งและรหัสรายการ จึงค้นด้วยชื่อได้และไม่เขียนทับกันเมื่อชื่อเหมือนกัน ไฟล์ JSON เป็นข้อมูลสำรองของระบบ ไม่ใช่ภาพสำหรับโพสต์ รูป PNG คือไฟล์ที่ใช้ทำคอนเทนต์ต่อ

## หากพบปัญหา

| อาการ | วิธีตรวจและแก้ |
| --- | --- |
| ระบบเบื้องหลังยังไม่พร้อม | ทำขั้นตอน 2–4 ให้ครบ โดยเฉพาะ setupWishesQueue หลังเว็บ Ready |
| setupWishesQueue ติดต่อ renderer ไม่ได้ | ตรวจ Production /api/wish-render ถูก Deploy แล้ว, URL ถูกต้อง, ไม่มี Deployment Protection ขวาง Google และ WISHES_SHARED_SECRET ตรงกันทั้งสองฝั่ง |
| ได้รับคำอวยพรแล้ว แต่ภาพยังไม่มา | ตรวจ Triggers และ Executions ของ processWishesQueue; รายการมีต้นฉบับแล้ว ไม่ต้องให้แขกส่งใหม่ |
| สถานะต้องตรวจสอบภาพ — ต้นฉบับยังอยู่ | ตรวจ Vercel Function Logs ของ /api/wish-render และ Apps Script Executions แก้สาเหตุก่อน แล้ว Run retryPendingWishes เพื่อให้คิวลองใหม่ |
| text-too-long | ข้อความยาวจนจัดลงพื้นที่การ์ดไม่ได้ ต้นฉบับยังอยู่ ให้เจ้าของนำข้อความไปจัดภาพเอง หรือแก้การจัดหน้าก่อนสั่งลองใหม่ ไม่ตัดคำอวยพรทิ้งอัตโนมัติ |
| font-unavailable / module not found | ตรวจอัปโหลด assets/fonts, package.json/lock และ lib ครบแล้ว และ Vercel ติดตั้ง npm สำเร็จ |
| โควตา Google เต็ม | รอการคืนโควตาแล้วตรวจคิว งานจะยังมีต้นฉบับอยู่ หากครบจำนวนลองใหม่ให้ Run retryPendingWishes |

บัญชี Google ทั่วไปมีโควตารวมการทำงานของ timed triggers ต่ำกว่าบัญชี Workspace ระบบนี้เหมาะกับงานที่มีปริมาณจำกัดและยอมให้ภาพทยอยเข้าภายหลัง ไม่ใช่บริการภาพทันทีแบบไม่จำกัด ควรทดลองหลายรายการก่อนวันงานและตรวจพื้นที่ Drive ให้เพียงพอ หลังงานจบและเก็บภาพครบ สามารถลบ trigger processWishesQueue จากหน้า Triggers ได้ หากเปิดรับอีกครั้งให้รัน setupWishesQueue ใหม่

## Assist และ Analytics

- แผนที่งาน: https://maps.app.goo.gl/GmbjmEJ2sR3kry6RA
- ห้องพัก: https://maps.app.goo.gl/oUeK93FsyzDybe6X7
- LINE พี่เบิร์ดและน้องแค้ปใช้ https://line.me/ti/p/s2AX8U__4j **ทั้งสองรายการตามลิงก์ที่ส่งมา** หากคนละบัญชี ให้เปลี่ยนใน js/bird-assistant.js และลิงก์สำรองใน index.html
- กำหนดการเปิดส่วนเดิมในเพจหลัก วันที่ 25 ตุลาคม 2569
- Analytics ใช้แพ็กเกจชื่อ **@vercel/analytics ตัวพิมพ์เล็ก** ซึ่ง build รวมไว้แล้ว ใน Vercel → โปรเจกต์ → Analytics ให้เปิด Enable หากยังไม่ได้เปิด แล้วเข้าชม Production เพื่อเริ่มมีข้อมูล ตัวบล็อกโฆษณาอาจกันการนับได้
- ไม่มี Analytics บน localhost และไม่เก็บ query string/hash ของ URL ใน page views

## ตรวจในเครื่อง

```sh
npm ci
npm run build
npm test
node tools/test-wish-render.cjs
python3 tools/validate.py
node server.mjs
```

เปิด URL ที่ server.mjs แสดง ระบบส่งเข้า Google ต้องมี environment variables เดิมของคุณจึงจะใช้งานจริงได้ การทดสอบอัตโนมัติใช้ข้อมูลจำลองและไม่เขียนเข้าชีตจริง

## ขอบเขตการตรวจของแพ็กเกจ

ตรวจการรับต้นฉบับถาวรด้วยระบบจำลอง การส่งซ้ำ การกลับมาประมวลผลต่อหลังแขกปิดหน้า การลองใหม่ การยืนยันเฉพาะเมื่อรับข้อมูลแล้ว การป้องกัน endpoint สร้างภาพ และสร้าง PNG จริงตรวจขนาด/300 DPI แล้ว ตรวจชุดทดสอบการ์ดและ dialog เดิมด้วย

ยังไม่ได้ Deploy แทนเจ้าของบัญชี ไม่ได้ทดสอบเขียน Google จริง ไม่ได้วัดเวลาบน Samsung Galaxy S22 หรือทดสอบภาพหน้าจอในเบราว์เซอร์มือถือจริง จึงไม่อ้างว่ารับข้อมูลได้ภายในจำนวนวินาทีตายตัว ให้ใช้ขั้นตอน 5 ตรวจหลังติดตั้งก่อนเปิดรับแขก
