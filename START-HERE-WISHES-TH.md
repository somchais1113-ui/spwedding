# PS Wedding v20 — เปิดรับคำอวยพรเข้า Google Sheets และ Drive

## สถานะของแพ็กเกจนี้
โค้ดพร้อมติดตั้ง ใช้ภาพการ์ดจาก v19 และคงระบบ RSVP เดิม ไม่ได้ Deploy หรืออนุญาต Google แทนเจ้าของบัญชี และยังไม่ได้ทดสอบส่งเข้า Google จริง
การเชื่อม Google Drive ในแชตไม่ใช่การให้สิทธิ์แก่เว็บไซต์ ต้องติดตั้ง Apps Script และตั้งค่า Vercel ตามด้านล่าง

## ปลายทางที่ผูกไว้
- Sheets: https://docs.google.com/spreadsheets/d/1He7PKjI5SvLfFaqupgbHIghvl7S2nM_QUg6JWdzztt0/edit
- Drive: https://drive.google.com/drive/folders/1Cp5hMHU6-OCN7xgqQhsO_bHgMlHPlXdu
- เครื่องมือคืนชื่อโฟลเดอร์ว่า Photo; ใช้ได้ ไม่ต้องเปลี่ยนชื่อ แต่ยังไม่ได้ยืนยันสิทธิ์เขียนแทนเจ้าของบัญชี
- เพิ่มเฉพาะแท็บ “คำอวยพร” และโฟลเดอร์ย่อย “การ์ดคำอวยพร”, “ลายมือต้นฉบับ”, “_wishes_uploads”
- ไม่ลบแท็บ RSVP ไม่เปลี่ยนสิทธิ์แชร์ของชีต/Drive; ไฟล์ใหม่สืบทอดสิทธิ์จากโฟลเดอร์ปลายทาง ตรวจว่าโฟลเดอร์ไม่เปิดสาธารณะหากต้องการเก็บเป็นส่วนตัว

## 1. เตรียมบัญชี
ใช้ Google บัญชีที่แก้ไขชีตและสร้างไฟล์ในโฟลเดอร์ปลายทางได้ทั้งสองแห่ง
ถ้าเจ้าของต่างบัญชี ให้แชร์สิทธิ์ Editor เฉพาะบัญชีที่จะใช้ติดตั้งสคริปต์ ไม่ต้องเปิด Anyone can edit

## 2. สร้าง Apps Script ใหม่สำหรับคำอวยพร
1. ไป https://script.google.com/ แล้วกด New project
2. ตั้งชื่อ PS Wedding Wishes
3. เปิดไฟล์ integrations/google-wishes/Code.gs จาก ZIP นี้ด้วยโปรแกรมแก้ไขข้อความ
4. คัดลอกทั้งไฟล์ วางแทนเนื้อหา Code.gs ในโปรเจกต์ใหม่ แล้ว Save
5. อย่าวางทับโปรเจกต์ RSVP เดิม และอย่าใช้ integrations/google-sheets/Code.gs ซึ่งเป็นของ RSVP

## 3. ตั้งค่าใน Apps Script
เปิด Project Settings (รูปเฟืองซ้าย) ตั้ง Time zone = Asia/Bangkok
ใน Script properties กด Add script property ใส่ทั้งสามแถวนี้ (ชื่อ Key ตรงตามตัวพิมพ์):

| Property | Value |
| --- | --- |
| WISHES_SHEET_ID | 1He7PKjI5SvLfFaqupgbHIghvl7S2nM_QUg6JWdzztt0 |
| WISHES_DRIVE_FOLDER_ID | 1Cp5hMHU6-OCN7xgqQhsO_bHgMlHPlXdu |
| WISHES_SHARED_SECRET | สร้างรหัสสุ่มใหม่อย่างน้อย 32 ตัวอักษรจากตัวจัดการรหัสผ่าน |

กด Save script properties ชีต/โฟลเดอร์มีค่าเริ่มต้นไว้แล้ว แต่การใส่ครบสามค่าอ่านและดูแลง่ายกว่า
รหัสลับต้องใช้ค่าเดียวกันใน Vercel ไม่ต้องส่งในแชต ห้ามใส่ใน index.html, js/config.js หรือ commit ลง GitHub
ไฟล์ appsscript.json ในโฟลเดอร์เดียวกันเป็นตัวอย่าง manifest; หากใช้ไฟล์นี้ ให้เปิด Show appsscript.json manifest ใน Project Settings แล้ววางลงไฟล์ manifest ไม่ใช่ Code.gs
ใช้ Apps Script V8 ตามค่าเริ่มต้น ไม่ต้องเปิด Advanced Drive API

## 4. รันตั้งค่าครั้งเดียว
1. กลับ Editor เลือกฟังก์ชัน setupWishes จากรายการด้านบน
2. กด Run ไม่ใช่ doPost (doPost ต้องมีคำขอจากเว็บ)
3. เลือก Review permissions → บัญชี Google เจ้าของระบบ → ตรวจและอนุญาต Sheets/Drive
4. ถ้ามีคำเตือน Google hasn't verified this app ให้ตรวจว่าชื่อโปรเจกต์/ผู้พัฒนาเป็นของคุณเองที่เพิ่งสร้าง หาก Google มี Advanced → Go to PS Wedding Wishes ให้ดำเนินต่อเฉพาะเมื่อยืนยันแล้ว ถ้าบัญชีองค์กรบล็อก ให้ผู้ดูแลอนุญาตก่อน
5. รอ Execution completed และข้อความ “พร้อมแล้ว: แท็บคำอวยพรและโฟลเดอร์ภาพ...”
6. เปิดชีตดูแท็บคำอวยพรและเปิด Drive ดูสามโฟลเดอร์ย่อย
รัน setupWishes ซ้ำได้ ไม่เพิ่มหัวคอลัมน์ซ้ำ หากแท็บเดิมมีหัวคอลัมน์ต่างรูปแบบจะหยุด ไม่ลบข้อมูลเดิม

## 5. Deploy ฝั่ง Google
1. Deploy → New deployment → Select type → Web app
2. Description: PS Wedding Wishes v20
3. Execute as: Me
4. Who has access: Anyone (แขกไม่ต้องล็อกอิน Google)
5. Deploy และ Copy Web app URL ซึ่งต้องลงท้าย /exec
6. เปิด URL ในแท็บใหม่ จะเห็น service: PS Wedding Wishes และ version: 20 เป็นการตรวจว่า Deploy ถูกโปรเจกต์เท่านั้น ยังไม่ยืนยันว่าเขียนข้อมูลได้
อย่าใช้ /dev, Script ID หรือ URL หน้า Editor
ไม่ต้องเผยแพร่ชีต/Drive เป็นสาธารณะ สคริปต์ตรวจรหัสลับก่อนรับคำขอและไม่มี API อ่านรายชื่อ/ข้อความ
เมื่อแก้ Code.gs ในอนาคต ให้ Deploy → Manage deployments → Edit → New version → Deploy เพื่ออัปเดต URL เดิม

## 6. ตั้ง Vercel
เปิดโปรเจกต์ spwedding → Settings → Environment Variables → Add Environment Variable

| Key | Value |
| --- | --- |
| WISHES_SCRIPT_URL | URL /exec จากข้อ 5 |
| WISHES_SHARED_SECRET | รหัสเดียวกับ Apps Script ทุกตัวอักษร |

เลือก Production สำหรับเว็บจริง ถ้าต้องทดสอบ Preview ให้เพิ่มค่าที่ Preview ด้วย
เก็บ RSVP_SCRIPT_URL และ RSVP_SHARED_SECRET เดิมไว้ ไม่ใช้ค่า WISHES แทน RSVP
ไม่ใส่เครื่องหมายคำพูดครอบค่า ไม่มีช่องว่าง/บรรทัดใหม่ท้ายค่า

## 7. อัปเดตเว็บ
1. แตก ZIP นี้ แล้วนำเนื้อหาทั้งโฟลเดอร์เข้า Repository เดิมที่ผูกกับ Vercel (ไม่อัปโหลด ZIP ให้กลายเป็นไฟล์ ZIP ใน repo)
2. ให้ index.html, api/, lib/, tools/, vercel.json อยู่ที่ Project Root เดิม ไม่ซ้อนโฟลเดอร์เพิ่ม
3. ห้ามอัปโหลดเฉพาะ public/ หรือ index.html เพราะจะขาด API
4. Framework: Other; Build Command: node tools/build-web.cjs; Output Directory: public
5. ใช้ Node.js 22 หรือ 24 ที่ Vercel รองรับ และ Fluid Compute; api/wishes.js ตั้ง maxDuration 60 วินาทีใน vercel.json
6. Commit/Push ให้เกิด Deployment ใหม่หลังตั้ง Env แล้ว หรือ Redeploy production ที่มีโค้ดชุดใหม่นี้
7. รอ Ready แล้วเปิด https://spwedding-teal.vercel.app/
server.mjs สำหรับรัน Node ใช้ตัวรับ Google ตัวเดียวกันแล้ว ต้องตั้ง WISHES_* เช่นกัน โค้ดรุ่นนี้ไม่บันทึกคำอวยพรใหม่ลง private-wishes แบบรุ่นเก่า ไฟล์เก่าไม่ถูกลบ

## 8. ทดสอบจริงก่อนใช้งาน
| ทดสอบ | สิ่งที่ต้องพบ |
| --- | --- |
| ชื่อ ทดสอบ-พิมพ์ และข้อความสั้น | ชีตมี 1 แถว ภาพในโฟลเดอร์การ์ดคำอวยพร สถานะบันทึกครบแล้ว |
| ชื่อ ทดสอบ-ลายมือ แล้วเขียนบนจอ | แถวมีลิงก์ภาพสองชนิด ชื่อไฟล์ขึ้นต้นด้วยชื่อผู้ส่ง |
| กดส่งซ้ำโดยไม่แก้ข้อความ | ใช้รหัสรายการเดิม ไม่สร้างไฟล์และแถวซ้ำ |
| ชื่อเหมือนกัน แต่เปลี่ยนข้อความ | สร้างคนละรายการ ไม่ทับคำอวยพรเดิม |
| ตัดเน็ตระหว่างส่ง แล้วต่อใหม่กดส่งอีกครั้ง | เก็บข้อมูลเดิม ใช้รหัสเดิม ตรวจจนภาพครบ |
| กลับมาเปิดใหม่จากเบราว์เซอร์เดิม | กู้ข้อความ/ลายมือและรหัสได้ถ้า localStorage ใช้งานได้และไม่ถูกล้าง |
| ส่งบน Samsung S22 และ iPhone | เปิดภาพที่ได้จาก Drive ตรวจชื่อ ภาษาไทย ความชัด และสัดส่วน |
| ทดลอง RSVP | ยังลงทะเบียนได้ตามเดิม |

ดูทั้งข้อความตอบกลับบนเว็บและข้อมูลในชีต/Drive ไม่ใช้แค่ HTTP 200 เป็นหลักฐานความสำเร็จ
ลบเฉพาะข้อมูลทดสอบที่คุณสร้างเมื่อจบทดสอบ

## หลังเปิดใช้งาน ข้อมูลอยู่ที่ไหน
ชีตแท็บคำอวยพร: รหัสรายการ / วันเวลาส่ง / ชื่อผู้ส่ง / ประเภท / ข้อความ / รูปแบบ / ลิงก์ลายมือ / ลิงก์การ์ด / สถานะ / อัปเดตล่าสุด
คอลัมน์ K–L เป็นข้อมูลระบบที่ซ่อนไว้ ห้ามแก้หรือลบเพื่อให้ส่งซ้ำ/รับภาพต่อได้ถูกต้อง
ภาพชื่อ เช่น สมชาย_2026-10-25_รหัสรายการ_การ์ด.png และ _ลายมือ.png
ชื่อไทย/อังกฤษคงไว้ เปลี่ยนเฉพาะอักขระที่ไม่เหมาะกับชื่อไฟล์ เช่น / เป็น _
ค้นหาชื่อในชีตหรือ Drive ได้โดยตรง ทุกไฟล์ผูกกับรหัสรายการเฉพาะ
โหมดลายมือเก็บภาพ ไม่ได้ถอดเป็นข้อความอัตโนมัติ

## ความละเอียดและพฤติกรรมเมื่อส่ง
- ภาพการ์ด PNG 300 DPI ใช้ renderer เดิม: แนวตั้ง 2400×3000 หรือแนวนอน 3200×1800
- ภาพลายมือ render จากเส้นที่เขียนเป็น PNG กว้าง 2400 px พร้อม 300 DPI
- ส่งชิ้นละ 2 MiB; JSON/base64 ต่อคำขอไม่เกิน 3 MiB; จำกัดไฟล์ละ 12 MiB
- ตรวจ SHA-256 ก่อนสร้างภาพสมบูรณ์ การแบ่งส่วนไม่ลดขนาดพิกเซล ไม่บีบอัดแบบสูญเสียรายละเอียด และไม่ลบ metadata DPI
- ไม่สร้างภาพขณะเปิด Landing; เตรียมเมื่อเข้าใกล้/ใช้ฟอร์ม
- ต้องเปิดหน้านี้ไว้ระหว่างส่ง ไม่มีคำสัญญาว่าจะส่งต่อเองหลังปิดเบราว์เซอร์
- ข้อความ/ชื่อบันทึกก่อนภาพ หน้าเว็บแสดงว่าครบแล้วเมื่อ Sheets มีลิงก์ครบเท่านั้น
- หากข้อความยาวจนใส่การ์ดไม่ได้ ข้อความที่รับแล้วคงอยู่ในชีต ส่วนภาพยังค้าง ต้องย่อข้อความแล้วส่งใหม่
- ป้องกันซ้ำด้วยรหัส ไม่ใช้ชื่อเป็นตัวตัดสิน หากล้างข้อมูลเบราว์เซอร์หรือเปลี่ยนเครื่อง รหัสอาจเป็นรายการใหม่
- หาก localStorage ถูกบล็อก กู้รหัสได้เฉพาะช่วงที่ยังเปิดหน้าเดิม ควรใช้เบราว์เซอร์ปกติ
- _wishes_uploads เก็บภาพระหว่างส่ง เมื่อสมบูรณ์ย้ายโฟลเดอร์ชั่วคราวเข้าถังขยะ; ภาพค้างไม่ถูกลบอัตโนมัติเพื่อให้ส่งต่อได้ หลังงานจึงค่อยตรวจและลบส่วนค้าง/ล้างถังขยะตามต้องการ
- ใช้พื้นที่และโควตาบัญชี Google เจ้าของระบบ ยังไม่ได้วัดความเร็วมือถือจริง

## แก้ปัญหาเบื้องต้น
| อาการ | ตรวจตรงไหน |
| --- | --- |
| ระบบรับคำอวยพรยังไม่ได้เปิดใช้งาน | Env WISHES_* อยู่ใน Production หรือไม่ และ Deploy ใหม่ที่มี api/wishes.js แล้วหรือยัง |
| setupWishes ขึ้น not_configured | Script property WISHES_SHARED_SECRET อย่างน้อย 32 ตัวอักษร |
| setupWishes หา Sheet/Folder ไม่เจอ | บัญชีที่รันต้องเข้าถึงสองปลายทาง และใส่ ID ไม่ใช่ URL |
| headers | แท็บคำอวยพรมีคอลัมน์ต่างจากแพ็กเกจ อย่าลบทับข้อมูลจริง ให้ตรวจชื่อคอลัมน์ก่อน |
| ข้อมูลไม่เข้าเลย | URL /exec เป็นของโปรเจกต์ Wishes, Execute as Me, access Anyone, secret ตรงกัน |
| ข้อความเข้า แต่ภาพยังไม่ครบ | รอก่อนแล้วกดส่งซ้ำโดยไม่แก้เนื้อหา ตรวจพื้นที่ Drive และ Apps Script → Executions |
| เปิด /exec ได้ แต่ส่งไม่สำเร็จ | GET ตรวจแค่เวอร์ชัน การเขียนต้องทดสอบจากฟอร์มจริง |
| ภาพเกิน 12 MB | ข้อมูลบันทึกไว้แล้ว ติดต่อผู้ดูแลเพื่อปรับวิธีรองรับ ห้ามลดคุณภาพเงียบ ๆ |
| rate limit | รออย่างน้อย 1 นาที จำกัดเบื้องต้น 120 requests/IP/นาทีต่อ instance, 60 รายการใหม่/นาทีรวม และ 2,000 รายการต่องาน |
| พร้อมรับแขกจำนวนมาก | Apps Script เขียนทีละรายการด้วย lock; ควรทดสอบพร้อมกันก่อนงาน ไม่มีการรับประกันรองรับทุกคนส่งพร้อมกันทันที |

ชื่อ Key ถูกต้องไม่มี NEXT_PUBLIC_ และห้ามเปิดเผย shared secret ใน screenshot/log
หากต้องส่งภาพหน้าจอมาตรวจ ให้ปิดค่ารหัสลับ ใช้ข้อความ error และสถานะ Deployment แทน

## ขอบเขตการตรวจสอบแพ็กเกจ
- ตรวจ syntax, asset references และ build
- unit tests ของ proxy / Apps Script ผ่านตัวจำลอง: auth, ชื่อซ้ำ, ส่งซ้ำ, ข้อความที่ดูเหมือนสูตร, ภาพไม่ครบ, chunk retry, checksum, กู้ไฟล์เมื่อเขียนชีตล้มเหลว
- ชุดทดสอบการ์ด 7 ข้อ, dialog 5 ข้อ, ระบบเดิมและ 300 DPI 6 ข้อ
- ยังไม่ได้ Deploy, เขียนข้อมูลลงบัญชี Google จริง หรือทดสอบบนมือถือจริง

เอกสาร Google: https://developers.google.com/apps-script/guides/web
https://developers.google.com/apps-script/guides/properties
https://developers.google.com/apps-script/reference/drive/folder
Vercel: https://vercel.com/docs/functions/limitations
