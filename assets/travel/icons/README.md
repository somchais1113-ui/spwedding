# UI icons
Small monochrome SVG stroke icons are defined in the `icons` object of js/travel/travel.js: compass, temple, coffee, mountain, leaf, camera, arrow. These are functional interface icons, not emoji. Colors inherit currentColor from the original wedding tokens. No external icon font is required. Use a 24 × 24 viewBox and 1.4 stroke if adding an icon.
